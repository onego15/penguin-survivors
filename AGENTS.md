# 開発ルール

- Compatibilityレンダラーで動作させる。
- 本編とサンドボックスの戦闘処理を共用する。ダメージ・状態異常・報酬・壁判定は既存の共通処理へ接続する。
- 演出だけの変更では攻撃性能・命中回数を変えない。予告は実判定に合わせ、敵の予告は黄色、発動中の危険は赤橙、味方は水色で区別する。
- ポーズ・武器選択・ボス演出中は戦闘と演出の時計を止める。死亡・勝利・再挑戦時に未発動の攻撃や一時効果を解除する。
- 性能値の正本はコード。仕様変更時は関連する `docs/` を更新する。READMEは入口に留め、詳細や変更履歴を積み増さない。説明文は日本語。

## 検証

- 挙動変更は対応テストと関連する回帰テストを実行する：`godot --headless --path . --script tests/<name>_test.gd`。終了コードだけでなくログの失敗・スクリプトエラーも確認する。
- 表示変更はCompatibilityの実画面で確認。ドキュメントのみならリンク・数値・表の整合性を確認する。
- 実行環境・テスト一覧・撮影手順は [docs/development.md](docs/development.md) を参照。自動操作の比較結果を実プレイの検証として扱わない。
